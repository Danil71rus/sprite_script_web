
module.exports = {
  serverFile: {
    port: 3000,
    host: "localhost", // "localhost" | "auto"  (задать свой или auto)
    dirProject: "dist",
  },
  client: {
    port: 3000, // необязательно, только во время разработки
  }
};
